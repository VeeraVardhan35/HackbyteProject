{
  "name": "@lcn/spacetime-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@lcn/types": "file:../../packages/types",
    "spacetimedb": "^2.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

